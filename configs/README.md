# Configurations

`data_sft.yaml` controls validation, deterministic trajectory selection, tokenizer auditing, and the SFT train/validation split. Training, sampling, DPO, and evaluation configurations will be added in their corresponding implementation phases. Machine-specific paths and secrets must stay out of these files.
